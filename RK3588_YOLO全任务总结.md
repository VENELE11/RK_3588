# RK3588 YOLO 项目 · 全任务总结

> 汇总周期：本次会话全部工作
> 目标平台：RK3588（三核 NPU @1GHz，librknnrt 1.5.3b6 / driver 0.7.2，rknn-toolkit2 2.3.2）
> 板端：`firefly@192.168.10.72`（Debian 11，aarch64）

---

## 一、任务全景

| # | 任务 | 状态 | 关键产出 |
|---|------|:---:|------|
| 1 | 阅读项目文档，理解背景 | ✅ | 掌握 RK3588 环境与基线 |
| 2 | 连接主板，实跑 YOLOv5 | ✅ | 78.11 FPS 完整视频处理 |
| 3 | 技术原理讲解（6 大主题） | ✅ | 硬件/部署/工具/流水线/线程模型/源码精讲 |
| 4 | 多模型性能对比（阶段 1-3） | ✅ | 性能表 + mAP 精度表 |
| 5 | 性能优化（P0-P3） | ✅ | v11s 修复 + 推理优化 |
| 6 | 总结与报告 | ✅ | 本报告 + 优化报告 |

---

## 二、任务 1-2：环境接入与 YOLOv5 实跑

### 2.1 连接方案
- 无 sshpass → 用 `expect` 密码自动登录（沙箱需放宽 pty 权限）
- 复杂脚本用 base64 编码传输规避 Tcl 转义
- `nohup` 后台运行 + 日志轮询解决 SSH 会话非持久

### 2.2 实测结果（`yolov5_thread_pool` 4 线程，720p60hz.mp4 5400 帧）
| 指标 | 值 |
|------|:---:|
| 平均端到端 FPS | **78.11**（68 个统计周期，71~85 波动） |
| 处理帧数 | 5392/5400 |
| 运行时 | API 1.5.3b6 / Driver 0.7.2 |
| 与文档基线 | ~79 FPS ✅ 吻合 |

---

## 三、任务 3：技术原理讲解

按计划分六部分，全部结合板端实际源码与实测：

1. **硬件与运行时原理**：三核 NPU 架构、devfreq 调频（1GHz）、RKNN 软件栈分层、`.rknn` 文件本质、`rknn_run` 调用链、多核并行规律成因
2. **模型部署全流程**：ONNX → config(mean/std) → load → build(INT8 量化+校准集) → export；板端实测文件全清单（onnx/rlym/slz/rknn）
3. **测试工具原理**：`rknn_benchmark`（gettimeofday 计时、core_mask 控核、warmup）、yolo11 demo（RGA 预处理、zero_copy）、两类口径对比
4. **端到端应用原理**：yolov5 线程池三线程架构、流水线重叠、队列背压、乱序处理
5. **线程数原理**：实测 1-5 线程扫描（15.9→42.8→68.1→76.6→81.0 FPS），证明"4 线程"是流水线填充而非核数对应
6. **源码精讲**：`yolov5_detect.cpp`（生产者-消费者编排）、`yolov5_thread_pool.cpp`（无锁推理区设计）

---

## 四、任务 4：多模型性能对比（阶段 1-3）

### 4.1 阶段 1：盘点
- 三模型文件齐全（v5s/v8s/v11s 的 .rknn）
- **板端有外网**（COCO 服务器可达）→ 数据可直接获取
- 磁盘 16G 可用、内存 15G

### 4.2 阶段 2：性能对比（统一口径实测）

| 模型 | 三核延迟 | 单核延迟 | 多核收益 | 端到端 FPS |
|------|:---:|:---:|:---:|:---:|
| YOLOv5s | 32.98ms | **29.14ms** | 负（单核最优） | 76.6~81.0（4/5线程） |
| YOLOv8s | **39.18ms** | 43.99ms | 正（~12%） | **87.6** |
| YOLOv11s | 84.03ms | **80.48ms** | 负（多核更慢） | 单图 416ms |

> 补充口径：统一单图端到端（Python）v5s 79ms / v8s 87ms / v11s 147ms——三模型排序在所有口径下一致。

### 4.3 阶段 3：精度评估（COCO val 100 张子集，mAP@0.5）

⚠️ **评估链路 bug 发现与修复**：COCO `category_id` 有跳号（80 类分布在 1~90），原脚本 `cid+1` 导致类别错位，初期 mAP 全面虚低（0.08~0.10）。修复后用真实 id 映射，并经 **pycocotools 官方 COCOeval 交叉验证**（误差 <0.003）：

| 模型 | mAP@0.5（修正后） |
|------|:---:|
| v5s（INT8 logit 版） | **0.5856** |
| v8s（INT8 6张量版） | **0.6120** |
| v11s（原始量化失真） | 0.0000 |

---

## 五、任务 5：性能优化（P0-P3）

### 5.1 P0：v11s 修复 —— 从"废模型"到可用 🎯

**根因链**（三层问题叠加）：
1. **输出量化失真**：box(0~640) 与 cls(0~1) 混用一个 scale=2.529 → cls 概率被量化压成 0 → mAP=0
2. **rknn 输出层 bug**：对 axis=2（最后一维）的 Slice 处理错误 → obj/cls 通道全 0（CPU 回退）
3. **官方 demo 静默失败**：yolo11 demo 期望 3 分支输出，对 `[1,8400,84]` 单张量布局分支索引越界，静默返回 0 检出

**修复组合拳**：
| 步骤 | 操作 |
|------|------|
| ① ONNX 输出层 box 通道 ÷640 归一化 | Slice(0:4)→Div(640)→Concat，输出值域统一 0~1 |
| ② 布局修复 | `[1,8400,84]`→Transpose→`[1,84,8400]` 再 Slice（axis=1），绕开 rknn bug |
| ③ 校准集升级 | 20→100 张 COCO |
| ④ v8s 同构修复 | opset 17→13 降级（本地 onnx 1.22 删 allowzero）+ box 归一化 |

**结果**：`yolov11s_norm2.rknn` mAP **0 → 0.4527**

### 5.2 P1：校准集升级（20→100）—— 结论：无益

| 模型 | 原版 | 新版 | 结论 |
|------|:---:|:---:|------|
| v5s | 0.5856 | 0.5842 (FP16) | 持平 |
| v8s | **0.6120** | 0.4959 | **下降**（Concat 融合输出共用 scale 有损失） |
| v11s | 0 | 0.4527 | 大幅提升 |

> 教训：解耦多输出（各分支独立量化）优于融合输出；能用的模型不要重转。

### 5.3 P2：Python 推理优化

| 优化 | 效果 |
|------|------|
| **uint8 直传**（免 float32 转换） | 41.6→36.4ms（**-12%**），输出逐位一致 |
| 后处理向量化（numpy 批量） | 消除逐元素循环 |
| lite2 运行时 | 36.8ms 持平，不采用 |

### 5.4 P3：批处理与并行

| 方案 | 结果 |
|------|------|
| 固定 batch=4 模型转换 | ❌ 不可行（onnxruntime 图验证对 batch 修改失败） |
| **Python 多实例并行**（4 线程） | ✅ **14.8 → 66.6 FPS（4.5×）** |

---

## 六、最终成果总表

| 模型 | 三核延迟 | mAP@0.5 | 端到端 | 综合评价 |
|------|:---:|:---:|:---:|------|
| **v8s** | 39.2ms | **0.6120** | 87 FPS | 🏆 精度最高、吞吐最高 |
| **v5s** | 33.0ms | 0.5856 | ~80 FPS | 🏆 速度最快 |
| **v11s（修复版）** | 84.0ms | 0.4527 | 66.6 FPS（Python多线程） | 已修复，速度受限 |

**综合结论**：720p 实时检测选 **v8s**（精度 0.612 + 87 FPS）；v11s 从完全不可用修复到 0.45 精度，但 C2PSA 结构在 NPU 上速度硬伤仍在。

---

## 七、关键经验（踩坑宝典）

1. **COCO category_id 跳号**：评估必须用 GT json 的真实 id 映射（本地 pycocotools 可交叉验证）
2. **rknn 2.3.2 输出层 axis=2 Slice bug**：通道被置 0，需先 Transpose 到 axis=1
3. **融合输出量化损失**：box(0~640)+cls(0~1) 共用 scale 是 mAP=0 类问题的通用根因；解耦多输出更优
4. **v5s 解码版 ONNX 的 INT8 量化 bug**（obj/cls 置 0，FP16 正常）
5. **mmse 量化+100 校准板端不可行**（22h+）；normal+100 校准仅 3~8 分钟
6. **固定 batch 模型不可行**：替代方案为多实例并行（C++ 79~87 FPS、Python 66.6 FPS）
7. **lite2 无速度收益**
8. **onnx opset 限制**：rknn 2.3.2 要求 ≤15；opset 17 需本地降级（删 allowzero 等）

---

## 八、产物清单

### 本地（`~/Downloads/`）
| 文件 | 说明 |
|------|------|
| `RK3588_YOLO优化报告.md` | 优化专项报告 |
| `eval_detect.py` | 三模型批量推理评估（uint8 直传 + 真实 category_id 映射） |
| `eval_map.py` | mAP@0.5 计算（101 点插值） |
| `coco_val_subset/` | COCO 100 张子集（图片+标注） |
| `ssh_rk3588.exp` / `scp_rk3588.exp` | 板端连接/传输工具 |
| `yolov8s_norm_op13.onnx` 等 | 转换中间产物 |

### 板端（`/home/firefly/...`）
| 产物 | 路径 | 说明 |
|------|------|------|
| v11s 修复模型 | `model/yolov11s/yolov11s_norm2.rknn` | mAP 0→0.4527 |
| v11s 修复 ONNX | `model/yolov11s/yolov11s_T_norm2.onnx` | box 归一化+Transpose |
| 评估/转换脚本 | `/tmp/eval_detect.py` `/tmp/eval_map.py` 等 | 全套工具链 |
| COCO 子集+校准集 | `/tmp/coco_eval/` | 100 图+标注+dataset_100.txt |

---

## 九、遗留与建议

1. **v11s 进一步优化**：hybrid 量化（C2PSA 层 FP16）可能同时改善精度与速度（尚未实施，mmse 尝试已证板端不可行）
2. **C++ 侧落地修复版 v11s**：新模型布局 `[1,84,8400]` 需匹配的新后处理，现有 demo 不兼容
3. **完整 COCO val（5000 张）评估**：100 张子集方差大，若需精确报告可用完整 val
4. **batch 场景**：若未来必须批处理，需在 PC 端转换或使用动态输入（有性能折损）
5. **板端杂物清理**：`/tmp` 下有大量中间产物（多版本 rknn/onnx/log），可清理释放空间

## 当前统一 500 张验收基线（2026-09-02）

后续精度判断统一采用 COCO val2017 500 张子集（`/tmp/coco_eval_500/`，标注 `coco_val_subset_500.json`，列表 `image_list_500.txt`）。此前 100 张结果保留为历史阶段记录，不作为当前验收结论。

| 模型 | AP@[0.5:0.95] | FPS | 用途 |
|---|---:|---:|---|
| ONNX FP32 | 48.72% | — | 精度基准 |
| RKNN FP16 | 48.65% | 5.63 | 部署精度基准 |
| 当前 INT8 | 15.6%～16.3% | 约 16.5 | 待修复 |

INT8 第一阶段验收目标：AP ≥45%，且与 RKNN FP16 的差距不超过 3 个百分点。执行优先级为：输入 dtype 验证 → 原始多尺度检测头 → C++ float32 DFL/解码 → FP16 对齐 → 默认 INT8/MMSE/per-channel → 逐层 hybrid quant → 必要时 QAT。
## 2026-09-02 原始解耦头首轮实测

板端从 RKNN Model Zoo 获取 YOLO11 解耦 ONNX，输出为 9 个张量：三个尺度各包含回归 `[1,64,H,W]`、分类 `[1,80,H,W]`、score `[1,1,H,W]`。使用 500 张 `calibration_letterbox_500.txt` 校准并统一 114 灰边/conf=0.01/NMS=0.45 评测：FP16 AP@[0.5:0.95]=0.4861，INT8=0.4803，AP50/AP75=0.6531/0.5218。INT8 相比 FP16 下降 0.59 个百分点，超过第一阶段目标；纯 NPU Python 100 次推理约 15.42 FPS。

该结果说明主要问题确实来自融合输出图的量化方式，而不是 YOLO11 主干本身。后续优先查询 9 个输出的逐张量 `type/scale/zp`，再进行 MMSE、per-channel 和回归/分类 hybrid 消融。
## 2026-09-02 输入 dtype 验证结果

同一 `yolov11s_zoo_int8_letterbox500.rknn` 与同一张 640×640 RGB 输入对比：`uint8` 输入可正常推理；`float32` 输入触发 Toolkit2 错误 `current input size(4915200) > need input size(1228800)`。这确认该 INT8 模型要求 1,228,800 字节的 8-bit 输入，C++ 不能再无条件强制 float32 或依赖错误的 UINT8/INT8 解释。后续必须读取 `RKNN_QUERY_INPUT_ATTR` 的 `type/scale/zp`，分别实现 UINT8、INT8 手动量化和 pass-through 测试。
## 2026-09-02 三模型 500 张复测

使用同一板端评测脚本、500 张 COCO val2017 子集、114 灰边、conf=0.01、NMS=0.45：

| 模型 | AP@[0.5:0.95] | AP50 | AP75 | FPS |
|---|---:|---:|---:|---:|
| 原始解耦头 ONNX FP32 | 48.74% | 65.67% | 53.01% | — |
| 原始解耦头 RKNN FP16 | 48.61% | 65.67% | 52.98% | 5.91 |
| 原始解耦头 RKNN INT8 | 48.03% | 65.31% | 52.18% | 15.65 |

INT8 相比 FP16 下降 0.59 个百分点，满足 AP≥45% 且差距≤3个百分点的第一阶段目标。

## 2026-09-02 YOLOv5s / YOLOv8s / YOLO11s 同标准横向复测

统一实测条件：RK3588 板端、640×640、114 灰边 letterbox、RGB、uint8 NHWC、conf=0.01、NMS=0.45、500 张 COCO val2017 子集、官方 `pycocotools.COCOeval`，指标为 bbox AP@[0.50:0.95]。FPS 为板端 Python RKNN 纯推理吞吐（100 次、5 次 warmup），不含图像预处理和后处理。

| 模型 | 官方 COCO AP@[.50:.95] | 板端 AP@[.50:.95] | 板端 AP50 | 板端 AP75 | 板端纯 NPU FPS |
|---|---:|---:|---:|---:|---:|
| YOLOv5s | 37.4% | 41.28% | 59.91% | 45.01% | 5.81 |
| YOLOv8s | 44.9% | 45.35% | 61.57% | 48.80% | 16.90 |
| YOLO11s | 47.0% | 48.03% | 65.31% | 52.18% | 15.91 |

官方数据为 Ultralytics 640px、COCO val2017 单模型单尺度结果；官方速度是 CPU ONNX/A100 或 TensorRT 等不同平台数据，不能直接与 RK3588 NPU FPS 数值相减比较。板端三模型均为当前可用 RKNN INT8 产物，其中 YOLO11s 使用原始解耦检测头版本。
