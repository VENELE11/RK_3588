# RK3588 YOLO 性能优化 · 四部分实测数据明细

> 记录日期：2026-08-17
> 环境：RK3588（三核 NPU @1GHz，librknnrt 2.3.2 / driver 0.7.2）
> 覆盖优化项：① 多线程流水线 ② uint8 直接输入 ③ 后处理向量化 ④ 多模型实例并行

---

## 一、多线程流水线（C++ `yolov5_thread_pool`）

### 1.1 线程数扫描实测

**条件**：同一 300 帧短视频（1280×720@60fps）、同一 `yolov5s.rknn`，仅改变线程数参数。

| 线程数 | 平均 FPS | 样本数 | 单样本波动区间 | 相对上档增量 |
|:---:|:---:|:---:|:---:|:---:|
| 1 | 15.88 | 18 | 13.6 ~ 20.7 | — |
| 2 | 42.79 | 6 | 40.4 ~ 44.5 | +26.9 |
| 3 | 68.10 | 4 | 61.6 ~ 72.8 | +25.3 |
| 4 | 76.61 | 3 | 70.8 ~ 81.6 | +8.5 |
| 5 | 81.02 | 3 | 74.3 ~ 88.6 | +4.4 |

**理论上限推算**：v5s 单核推理 34.8ms → 每核吞吐 28.7 FPS → 三核饱和 ≈ 86 FPS。
5 线程 81.02 FPS = 硬件上限的 **94%**。

### 1.2 完整视频验证

**条件**：`720p60hz.mp4`（1280×720@60fps，5400 帧），4 线程。

| 指标 | 值 |
|------|:---:|
| 平均端到端 FPS | **78.11** |
| 统计周期 | 68 个（每秒一个） |
| 波动区间 | 71 ~ 85 FPS |
| 处理帧数 | 5392 / 5400 |
| 处理耗时 | 68s（处理 90s 视频，快于实时） |

### 1.3 对照：v8s 端到端

**条件**：`rknn_deploy`（内部 6 线程），300 帧短视频。

| 模型 | 端到端 FPS |
|------|:---:|
| YOLOv8s | **87.57** |

---

## 二、uint8 直接输入（Python 推理）

### 2.1 计时对比

**条件**：同一 v5s 模型、同一输入图、预热后 10 次推理取均值。

| 输入类型 | 平均耗时/次 | 说明 |
|---------|:---:|------|
| float32（原方案） | **41.59 ms** | runtime 内部需 float32→INT8 转换，报 `input size(4915200) > need(1228800)` 警告 |
| **uint8 直传** | **36.43 ms** | 免转换，无警告 |

**提升：-5.16 ms/次（-12.4%）**

### 2.2 正确性验证

10 次推理，float32 与 uint8 输出逐位对比：

| 输出张量 | max abs diff |
|:---:|:---:|
| out0 | 0.0 |
| out1 | 0.0 |
| out2 | 0.0 |

> 输出完全一致（uint8 直传无损，因为模型输入本身就是 INT8 + 归一化已融合）。

---

## 三、后处理向量化

**性质说明**：代码结构性优化，与 uint8 直传一并落地，未单独分离测时——以下为改动清单，效果体现在整体评估链路吞吐中。

| 改动点 | 之前（逐元素循环） | 之后（numpy 批量） |
|--------|------|------|
| `post_v5s` 候选收集 | `for k in range(bx.shape[0]): dets.append(...)`（每候选一次 Python 循环） | `np.stack(...)` + `np.concatenate(pieces, 0)` 一次性拼接 |
| `post_v8s` / `post_v11s` | 同上逐元素 append | 同上向量化 |
| `apply_nms` | 仅接受 list、内部转数组 | 直接接受 ndarray，避免重复转换 |

**收益特性**：候选越多收益越大——评估采用 conf=0.01，每图候选数百~数千（v11s 修复版最多 8400 个），向量化消除了 Python 逐元素循环的 O(N) 解释器开销。

**合并效果参考**（uint8 + 向量化共同作用）：100 张 COCO 图批量评估，单模型耗时从初版明显下降（初版含逐元素循环与 float32 转换，未做分段计时）。

---

## 四、多模型实例并行（Python 4 线程）

### 4.1 实测数据

**条件**：同一 v5s 模型、100 张随机图（uint8 输入）、4 个独立 RKNN 实例并行。

| 方案 | 总耗时 | 吞吐 |
|------|:---:|:---:|
| 单线程（1 实例顺序推理） | 6.76 s | **14.8 FPS** |
| **4 线程（4 实例并行）** | 1.50 s | **66.6 FPS** |

**加速比：4.50×**

### 4.2 机制说明

- 每个线程加载**独立 RKNN 实例**（独立 context，各自 `rknn_init`）
- `rknn.inference` 为 C 扩展调用且**释放 GIL** → 4 线程真正并行，任务喂给 NPU 三核
- 对应 C++ 线程池设计（`yolov5_thread_pool.cpp`：每 worker 独立 `Yolov5` 实例）

### 4.3 与 C++ 对照

| 实现 | 口径 | 吞吐 |
|------|------|:---:|
| C++ 线程池（4 线程） | 完整视频端到端（含解码/预处理/后处理流水线重叠） | **78.11 FPS** |
| Python 4 线程 | 纯并行推理（无流水线重叠、含 Python 层开销） | **66.6 FPS** |

---

## 五、优化贡献汇总

| 优化项 | 提升 | 测量口径 |
|--------|:---:|------|
| 多线程流水线（1→4 线程，C++） | 15.9 → 76.6 FPS（**4.8×**） | 端到端视频 |
| uint8 直传 | 41.6 → 36.4 ms（**-12.4%**） | Python 单次推理 |
| 后处理向量化 | 结构性优化（未单独计时） | 代码级 |
| 多实例并行（1→4 线程，Python） | 14.8 → 66.6 FPS（**4.5×**） | 纯推理吞吐 |

---

## 附：复现命令

```bash
# 1. 多线程扫描（板端，300 帧短视频 /tmp/short_300.mp4）
cd /home/firefly/Tingshuo/rknn-3588-npu-yolo-accelerate
for T in 1 2 3 4 5; do ./build/yolov5_thread_pool ./weights/yolov5s.rknn /tmp/short_300.mp4 $T; done

# 2. uint8 直传对比（板端 python）
#    见项目文件夹 eval_detect.py 的 inference 调用（astype(np.uint8)[None]）

# 4. 多实例并行（板端 python）
#    见 /tmp/bench_multithread.py（单线程 vs 4 线程 100 张图计时）
```

---

## 六、C++ 后处理专项优化（2026-08-31）

### 6.1 分阶段基线

使用COCO 100张子集、OpenCV统一预处理和量化输出，统计每张平均耗时：

| 模型 | 预处理 | 输入提交 | NPU前向 | 输出读取 | 后处理 | 端到端 |
|---|---:|---:|---:|---:|---:|---:|
| v5 | 5.55 ms | 0.76 ms | 35.73 ms | 4.53 ms | 9.03 ms | 55.60 ms |
| v8 | 5.32 ms | 0.65 ms | 44.40 ms | 3.27 ms | 14.44 ms | 68.07 ms |
| v11_norm2 | 6.76 ms | 0.54 ms | 61.53 ms | 0.59 ms | 16.93 ms | 86.35 ms |

结论：NPU前向是第一瓶颈，CPU后处理是第二瓶颈；v8/v11的主要CPU成本是扫描8400×80个类别值，v8还需执行DFL softmax。

### 6.2 已实施优化

- Top-500前移，仅为入选候选执行框解码和DFL。
- INT8/UINT8整数域argmax，最大类别确定后仅反量化一次。
- 确定性Top-K，保持与Python稳定排序一致。
- 80类固定数组NMS，去除`std::map`、重复排序和重复面积计算。
- 候选、检测结果容器提前预留容量。

### 6.3 结果

| 模型 | 后处理优化前 | 后处理优化后（保守连续值） | 降幅 | 输出一致性 |
|---|---:|---:|---:|---|
| v5 | 9.03 ms | 2.51 ms | 72.2% | JSON逐值一致 |
| v8 | 14.44 ms | 8.75 ms | 39.4% | JSON逐值一致 |
| v11_norm2 | 16.93 ms | 13.45 ms | 20.5% | JSON逐值一致 |

独立运行最低实测：v5 2.30 ms、v8 8.57 ms、v11 12.64 ms。

精度保持不变：v5/v8/v11的mAP@0.5分别为0.5857、0.6112、0.4439。300帧v5视频实测为单线程15.74 FPS、5线程83.80 FPS。

### 6.4 零拷贝与 ARM NEON 复测

在后处理优化基础上，进一步启用 RKNN 固定 IO 内存和量化输出缓冲区复用。输入使用 `rknn_create_mem` 分配一次，逐帧拷贝后 `rknn_mem_sync(..., RKNN_MEMORY_SYNC_TO_DEVICE)`；推理后对固定输出缓冲区执行 `RKNN_MEMORY_SYNC_FROM_DEVICE`，直接读取 `virt_addr`，从而取消每帧 `rknn_outputs_get/release`。

v8/v11 通道优先输出的类别 argmax 使用 ARM NEON，每次并行比较 16 个网格位置，并以“严格大于”更新，保持 Python/C++ 的相等值优先级一致。v5 的目标通道筛选较稀疏，实测 NEON 全量扫描会使后处理从约 2.5 ms 回退到约 10 ms，因此 v5 保留目标筛选优先的标量实现。

最终连续复测（100 张 COCO 子集）：

| 模型 | 预处理 | 输入同步 | NPU前向 | 输出同步 | 后处理 | 端到端 | FPS |
|---|---:|---:|---:|---:|---:|---:|---:|
| v5 | 5.579 ms | 0.491 ms | 38.443 ms | 0.144 ms | 2.721 ms | 47.378 ms | 21.107 |
| v8 | 5.300 ms | 0.474 ms | 46.599 ms | 0.111 ms | 5.409 ms | 57.894 ms | 17.273 |
| v11_norm2 | 6.501 ms | 0.448 ms | 61.015 ms | 0.083 ms | 4.206 ms | 72.254 ms | 13.840 |

最终输出验证：v5/v8/v11 均为 2124/2179/3354 个预测，mAP@0.5 为 0.5857/0.6112/0.4439；与已完成 Python/C++ 对齐的基线逐框匹配，数量一致，分数和坐标仅保留浮点末位误差。300 帧视频复测为单线程 15.51 FPS、5 线程 86.81 FPS，平均检测框 51.757/帧。

### 6.5 预处理与数据传输优化

进一步把 OpenCV/RGA 的目标画布直接绑定到 RKNN 输入缓冲区：OpenCV 通过预分配的输入 `cv::Mat` 完成颜色转换，RGA 直接把缩放结果写入该缓冲区，去掉独立 RGB 画布到 RKNN 缓冲区的二次拷贝；输入只保留一次 `RKNN_MEMORY_SYNC_TO_DEVICE`。

相比 6.4 的固定缓冲区版本，最终连续复测如下：

| 模型 | 预处理 | 输入同步 | NPU前向 | 输出同步 | 后处理 | 端到端 | FPS |
|---|---:|---:|---:|---:|---:|---:|---:|
| v5 | 3.004 ms | 0.112 ms | 39.155 ms | 0.146 ms | 2.674 ms | 45.090 ms | 22.178 |
| v8 | 3.931 ms | 0.096 ms | 46.323 ms | 0.111 ms | 5.445 ms | 55.907 ms | 17.887 |
| v11_norm2 | 4.356 ms | 0.088 ms | 62.482 ms | 0.078 ms | 4.301 ms | 71.306 ms | 14.024 |

相对 6.4，预处理下降约 26%~46%，输入同步下降约 77%~80%；相对最初基线，端到端耗时下降约 17%~19%，预测数量、mAP 和框结果保持不变。

### 6.6 统一多线程框架

`yolov5_rknn_multi` 已从 v5 专用入口统一为 v5/v8/v11/v11_norm 通用入口。每个 Worker 独立创建 RKNN context、IO 内存和预处理缓存，并按模型类型调用已有解码器；有界任务队列限制待处理帧数量，Worker 异常通过共享状态通知主线程，避免线程异常直接导致进程终止。

统一入口：

```bash
./build/yolov5_rknn_multi v5 model.rknn video.mp4 5 300
./build/yolov5_rknn_multi v8 model.rknn video.mp4 5 300
./build/yolov5_rknn_multi v11_norm model.rknn video.mp4 5 300
```

板端实测：v5 5 线程处理 300 帧为 92.93 FPS、平均 51.757 个检测框/帧；v8 5 线程处理 60 帧为 69.78 FPS、平均 34.417 个检测框/帧；v11_norm 5 线程处理 60 帧为 54.74 FPS、平均 42.817 个检测框/帧。框架同时输出各 Worker 汇总后的预处理、输入同步、NPU 前向、输出同步和后处理耗时。

## 7. Python/C++ 完整测试矩阵（2026-08-31）

### 7.1 测试口径

测试在 RK3588 板端进行。Python 使用 toolkit2.3.2；图片为 COCO 100 张子集，视频为 `720p60hz.mp4` 前 300 帧。速度为端到端吞吐，包含图片读取或视频解码、预处理、RKNN 推理和后处理。单进程使用 1 个 RKNN 实例，多 worker 使用 5 个独立 RKNN 实例。

### 7.2 准确度

| 模型 | Python预测数 | C++预测数 | Python mAP@0.5 | C++ mAP@0.5 | 对齐结果 |
|---|---:|---:|---:|---:|---|
| v5s | 2124 | 2124 | 0.5857 | 0.5857 | 逐框匹配，最小 IoU 0.99998 |
| v8s | 2179 | 2179 | 0.6112 | 0.6112 | 全部 IoU≥0.5，2178/2179 IoU≥0.99 |
| v11s_norm2 | 3354 | 3354 | 0.4439 | 0.4439 | 数量一致，浮点末位误差 |

### 7.3 单进程速度

| 模型 | Python 图片 FPS | C++ 图片 FPS | Python 视频 FPS | C++ 视频 FPS |
|---|---:|---:|---:|---:|
| v5s | 9.07 | 22.46 | 9.86 | 17.02 |
| v8s | 8.21 | 17.58 | 8.27 | 13.55 |
| v11s_norm2 | 7.66 | 13.62 | 8.30 | 12.27 |

### 7.4 五 worker 速度

| 模型 | Python 图片 FPS | C++ 图片 FPS | Python 视频 FPS | C++ 视频 FPS |
|---|---:|---:|---:|---:|
| v5s | 44.69 | 99.25 | 41.64 | 92.70 |
| v8s | 49.32 | 78.74 | 47.72 | 76.49 |
| v11s_norm2 | 49.97 | 58.36 | 47.57 | 56.11 |

结论：当前 Python 的优化已在 C++ 中完成等价实现并通过准确度对齐。C++ 单进程端到端吞吐约为 Python 的 1.48～2.48 倍；5 worker 下约为 1.18～2.23 倍。v11 多 worker 的提升较小，说明此时更接近 NPU 计算和共享带宽瓶颈。

Python 端到端基准脚本为项目根目录的 `bench_python_pipeline.py`，板端副本为 `/tmp/bench_python_pipeline.py`；C++ 图片多 worker 使用 `yolov5_rknn_multi ... --images ...`。

## 2026-09-01 新 IP 板端 C++ 后处理复测

测试板端：`firefly@192.168.10.72`；测试集：`/tmp/coco_eval/` 中 COCO val 100 张子集；后处理：C++，使用 `yolov5_rknn`（v5）和 `yolo_modern`（v8/v11_norm）；输入统一为 640×640、letterbox、阈值 0.01、NMS 0.45。

| 模型 | C++ 预测数 | C++ mAP@0.5 | 端到端耗时 | 端到端 FPS |
|---|---:|---:|---:|---:|
| v5s | 2124 | **0.5857** | 46.079 ms | 21.702 |
| v8s | 2179 | **0.6112** | 58.206 ms | 17.180 |
| v11s_norm2 | 3354 | **0.4439** | 57.779 ms | 17.307 |

与板端 Python 对齐结果：

- v5：2124/2124 框匹配，最小 IoU `0.999980`，最大分数误差 `5.96e-08`。
- v8：2179/2179 框匹配，2178 个 IoU ≥ `0.99`，全部 IoU ≥ `0.50`。
- v11_norm2：3354/3354 框匹配，3350 个 IoU ≥ `0.99`，最大坐标误差 `3.05e-05`；其余差异来自边界裁剪后的等价框表示。

复测结论：三个模型的 C++ 后处理精度均正常，mAP 与既有结果一致；v8s 仍为精度最高模型，v5s 速度优先，v11s_norm2 可用但精度和模型结构仍弱于 v5/v8。
## 2026-09-01 COCO 官方常用 AP 指标复测

已将 `eval_map.py` 更新为 COCO 常用指标：IoU 阈值 `0.50:0.05:0.95`，101 个召回率采样点，单图最多保留 100 个检测；脚本优先调用 `pycocotools.COCOeval`，板端未安装该库时使用同口径纯 Python bbox 回退实现，同时输出 AP@[.50:.95]、AP50 和 AP75。

本轮仍使用现有 COCO 100 张子集（项目中没有其余 COCO 图片，无法安全扩大测试集）：

| 模型 | AP@[.50:.95] | AP@.50 | AP@.75 |
|---|---:|---:|---:|
| v5s | **0.4013** | 0.5856 | 0.4322 |
| v8s | **0.4696** | 0.6092 | 0.4996 |
| v11s_norm2 | **0.2194** | 0.4439 | 0.1921 |

说明：本轮 C++ 预测数量仍为 v5/v8/v11 的 2124/2179/3354，AP@[.50:.95] 低于 AP50 是正常现象，因为它对 IoU=0.55~0.95 的定位精度提出了更严格要求。以上结果是 100 张子集结果，不代表完整 COCO val2017 官方成绩；板端本轮使用纯 Python COCO bbox 回退实现，安装 `pycocotools` 后可再做官方库交叉核验。
## 2026-09-01 COCO 500 张官方 COCOeval 复测

在原 100 张子集基础上，从 COCO val2017 补充下载 400 张图片，板端测试目录为 `/tmp/coco_eval_500/`，共 500 张图片。使用 C++ 后处理生成预测，并使用已安装的 `pycocotools.COCOeval` 计算官方常用指标 `AP@[0.50:0.95]`（area=all，maxDets=100）。

| 模型 | C++ 预测数 | AP@[.50:.95] | AP@.50 | AP@.75 |
|---|---:|---:|---:|---:|
| v5s | 10526 | **0.3998** | 0.5980 | 0.4372 |
| v8s | 10868 | **0.4538** | 0.6167 | 0.4875 |
| v11s_norm2 | 16808 | **0.1556** | 0.3768 | 0.1159 |

端到端 C++ 图片吞吐：v5s `21.844 FPS`，v8s `17.562 FPS`，v11s_norm2 `16.744 FPS`。500 张结果比 100 张子集更稳定：v5/v8 结果基本保持，v11 从子集 AP `0.2194` 降至 `0.1556`，此前 100 张结果对 v11 明显偏乐观。完整 COCO val2017 仍需其余图片全部可用后才能评估。
## v11s 低精度调研执行记录（2026-09-01）

- 固定 500 张 COCO val2017 子集作为基线；当前 `yolov11s_norm2.rknn`：AP@[0.5:0.95] 0.1556（15.56%），AP50 0.3768，AP75 0.1159，AP-small 0.072，预测框 16808 个。
- 开发板同时存在 `yolov11s_norm100.rknn`、`yolov11s_norm2.rknn` 及 `yolov11s_T_norm2.onnx` 等资产，但没有发现原始 `.pt/.pth` 权重，因此暂不能完成 PyTorch FP32 参照组。
- 首个量化消融：`yolov11s_norm100.rknn` 可加载且输出张量为 `[1,84,8400]`，但 500 张图全部产生 0 个有效检测，说明该变体不能直接作为正常精度模型使用，优先检查量化校准集、输出量化参数及模型导出配置。
- 已确认 C++ 后处理对 `norm2` 的输出布局 `[1,84,8400]` 处理正确；当前主要异常仍集中在模型/量化输出，而非 COCO 指标换算。

下一步：用同一批图片对 `T_norm2.onnx` 做 CPU 推理并复用相同解码，抽取输出范围、类别分数和框坐标，与 RKNN 输出逐层比较；随后检查 norm100 的量化参数和校准集。

### 阶段对照结果（500 张 COCO，2026-09-01）

| 阶段 | 输出/后处理 | AP@[0.5:0.95] | AP50 | AP75 | 预测框 |
|---|---|---:|---:|---:|---:|
| 原始 yolov11s_T.onnx | ONNX Runtime + C++ 等价解码逻辑 | 0.4906 | 0.6625 | 0.5334 | 48137 |
| yolov11s_T_norm2.onnx | ONNX Runtime + C++ 等价解码逻辑 | 0.4872 | 0.6546 | 0.5313 | 11349 |
| yolov11s_norm2.rknn | RKNN + C++ 后处理 | 0.1556 | 0.3768 | 0.1159 | 16808 |
| yolov11s_norm100.rknn | RKNN + C++ 后处理 | 无效 | 无效 | 无效 | 0 |

结论：ONNX 两个版本均约 49% AP，说明原始权重与导出网络基本正常；转换为 RKNN 后 AP 下降 33.16 个百分点，且 AP75 从 53.13% 降到 11.59%，同时预测框数量异常增加。因此首要根因是 RKNN 量化/校准或输出反量化参数，而不是 COCO 评估公式或 C++ 后处理。norm100 的全零检测进一步证明其校准配置不可用。

### 修复执行顺序

1. 使用与部署预处理完全一致的 500 张 COCO 图片生成 RKNN calibration dataset。
2. 先生成 RKNN FP16（do_quantization=False）并在同一 500 张集合上验证，目标接近 ONNX 的 0.48 AP。
3. 再生成 INT8，保持输入 mean/std 为 [0,0,0]/[255,255,255]，逐层检查输出 scale/zp；优先保留检测头输出为 FP16/混合精度。
4. 只有 RKNN 输出与 ONNX 的框坐标、类别分数分布接近后，才继续调阈值和 NMS。


### Toolkit2 重建结果（最终）

- 使用 Toolkit2 2.3.2、500 张原图校准重新生成 INT8：AP@[0.5:0.95] 0.1631，AP50 0.3820，AP75 0.1198。
- 使用 500 张与部署一致的 640 letterbox 图像重新生成 INT8：AP@[0.5:0.95] 0.1567，AP50 0.3751，AP75 0.1112。
- FP16 RKNN：AP@[0.5:0.95] 0.4865，AP50 0.6547，AP75 0.5306，基本保持 ONNX 精度。

最终判断：增加校准集、改用部署一致的 letterbox 校准均不能解决 INT8 精度问题；Toolkit2 量化过程中报告的 outlier 权重与检测头量化敏感性是主要嫌疑。当前推荐部署 FP16；若必须 INT8，应做混合精度/检测头保留 FP16，并进一步用 Toolkit2 accuracy_analysis/hybrid_quant 锁定敏感层。

另外修复了 C++ 后处理对 RKNN FLOAT16 输出的读取：新增半精度转 float，并统一通过 tensor_value 读取类别分数；该修复已同步到 cpp_port 源码。


### Hybrid quant 最终结果（Toolkit2 2.3.2）

- 仅将 new_output 保留 FP16：AP@[0.5:0.95] 0.1575，AP50 0.3762，AP75 0.1115。
- 将 model.23 三尺度检测头卷积、拼接、sigmoid 及 new_output 保留 FP16：AP@[0.5:0.95] 0.1582，AP50 0.3765，AP75 0.1155。
- 结论：检测头末端混合精度只带来约 0.26 个百分点提升，量化损失已在更早的主干/特征融合层产生。当前部署建议使用 yolov11s_fp16_500.rknn；若必须 INT8，需要扩大敏感层保留范围或采用量化感知训练，不能继续只调后处理阈值。


## 当前统一 500 张验收基线（2026-09-02）

后续精度判断统一采用 COCO val2017 500 张子集（`/tmp/coco_eval_500/`，标注 `coco_val_subset_500.json`，列表 `image_list_500.txt`）。此前 100 张结果保留为历史阶段记录，不作为当前验收结论。

| 模型 | AP@[0.5:0.95] | FPS | 用途 |
|---|---:|---:|---|
| ONNX FP32 | 48.72% | — | 精度基准 |
| RKNN FP16 | 48.65% | 5.63 | 部署精度基准 |
| 当前 INT8 | 15.6%～16.3% | 约 16.5 | 待修复 |

INT8 第一阶段验收目标：AP ≥45%，且与 RKNN FP16 的差距不超过 3 个百分点。执行优先级为：输入 dtype 验证 → 原始多尺度检测头 → C++ float32 DFL/解码 → FP16 对齐 → 默认 INT8/MMSE/per-channel → 逐层 hybrid quant → 必要时 QAT。
## 2026-09-02 板端环境核验

已通过交互式 SSH 核对 RK3588：

- 系统：Linux firefly 5.10.66，aarch64。
- Python：3.6.8；`rknn.api` 可导入。
- 当前评测集：`/tmp/coco_eval_500/`，存在 `coco_val_subset_500.json` 和 `image_list_500.txt`。
- YOLO11 目录：`/home/firefly/model/yolov11s/`，存在 `yolo11s.pt`、`yolov11s_T.onnx`、`yolov11s_T_norm2.onnx`、`yolov11s_fp16_500.rknn`、`yolov11s_int8_500.rknn` 以及 hybrid 版本。
- 板端没有安装 `ultralytics`，因此 `export_yolo11_raw.py` 暂不能直接运行；板端已有 RKNN Model Zoo 的 `examples/yolo11`，其模型结构已经是解耦的回归/分类输出，可优先复用其导出与转换链路。

因此当前 500 张验收集和既有 FP16/INT8 产物均已确认，下一步不是重新下载数据，而是基于 Model Zoo 的解耦输出或补齐主机端 Ultralytics 导出环境，生成三尺度 raw-head 模型。
## 2026-09-02 原始解耦头首轮实测

板端从 RKNN Model Zoo 获取 YOLO11 解耦 ONNX，输出为 9 个张量：三个尺度各包含回归 `[1,64,H,W]`、分类 `[1,80,H,W]`、score `[1,1,H,W]`。使用 500 张 `calibration_letterbox_500.txt` 校准并统一 114 灰边/conf=0.01/NMS=0.45 评测：FP16 AP@[0.5:0.95]=0.4861，INT8=0.4803，AP50/AP75=0.6531/0.5218。INT8 相比 FP16 下降 0.59 个百分点，超过第一阶段目标；纯 NPU Python 100 次推理约 15.42 FPS。

该结果说明主要问题确实来自融合输出图的量化方式，而不是 YOLO11 主干本身。后续优先查询 9 个输出的逐张量 `type/scale/zp`，再进行 MMSE、per-channel 和回归/分类 hybrid 消融。
## 2026-09-02 输入 dtype 验证结果

同一 `yolov11s_zoo_int8_letterbox500.rknn` 与同一张 640×640 RGB 输入对比：`uint8` 输入可正常推理；`float32` 输入触发 Toolkit2 错误 `current input size(4915200) > need input size(1228800)`。这确认该 INT8 模型要求 1,228,800 字节的 8-bit 输入，C++ 不能再无条件强制 float32 或依赖错误的 UINT8/INT8 解释。后续必须读取 `RKNN_QUERY_INPUT_ATTR` 的 `type/scale/zp`，分别实现 UINT8、INT8 手动量化和 pass-through 测试。
## 2026-09-02 三模型 500 张复测

使用同一板端评测脚本、500 张 COCO val2017 子集、114 灰边、conf=0.01、NMS=0.45：

| 模型 | AP@[0.5:0.95] | AP50 | AP75 | FPS |
|---|---:|---:|---:|---:|
| 原始解耦头 ONNX FP32 | 48.74% | 65.67% | 53.01% | — |
| 原始解耦头 RKNN FP16 | 48.61% | 65.67% | 52.98% | 5.91 |
| 原始解耦头 RKNN INT8 | 48.03% | 65.31% | 52.18% | 15.65 |

INT8 相比 FP16 下降 0.59 个百分点，满足 AP≥45% 且差距≤3个百分点的第一阶段目标。

## 2026-09-02 YOLOv5s / YOLOv8s / YOLO11s 同标准横向复测

统一实测条件：RK3588 板端、640×640、114 灰边 letterbox、RGB、uint8 NHWC、conf=0.01、NMS=0.45、500 张 COCO val2017 子集、官方 `pycocotools.COCOeval`，指标为 bbox AP@[0.50:0.95]。FPS 为板端 Python RKNN 纯推理吞吐（100 次、5 次 warmup），不含图像预处理和后处理。

| 模型 | 官方 COCO AP@[.50:.95] | 板端 AP@[.50:.95] | 板端 AP50 | 板端 AP75 | 板端纯 NPU FPS |
|---|---:|---:|---:|---:|---:|
| YOLOv5s | 37.4% | 41.28% | 59.91% | 45.01% | 5.81 |
| YOLOv8s | 44.9% | 45.35% | 61.57% | 48.80% | 16.90 |
| YOLO11s | 47.0% | 48.03% | 65.31% | 52.18% | 15.91 |

官方数据为 Ultralytics 640px、COCO val2017 单模型单尺度结果；官方速度是 CPU ONNX/A100 或 TensorRT 等不同平台数据，不能直接与 RK3588 NPU FPS 数值相减比较。板端三模型均为当前可用 RKNN INT8 产物，其中 YOLO11s 使用原始解耦检测头版本。
