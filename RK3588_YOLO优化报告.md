# RK3588 YOLO 多模型优化报告

> 记录日期：2026-08-17
> 前提：`RK3588_YOLO多模型性能对比.md`（基线数据）+ 本报告（优化成果）
> 测试环境：RK3588（三核 NPU @1GHz，librknnrt 1.5.3b6 / driver 0.7.2，rknn-toolkit2 2.3.2）

---

## 一、优化前基线（修正版）

### ⚠️ 关键修正：mAP 评估链路 bug

原报告中 mAP（v5s 0.0788 / v8s 0.0974）**错误**。根因：COCO 的 `category_id` 有跳号（80 类分布在 1~90 的 id 空间，id=12 缺失），原脚本用 `cid+1` 导致类别错位。修复后（按 GT json 的真实 category_id 映射）：

| 模型 | 旧值（错误） | 修正值 |
|------|:---:|:---:|
| v5s (INT8 logit) | 0.0788 | **0.5856** |
| v8s (INT8 6张量) | 0.0974 | **0.6120** |
| v11s (量化失真) | 0.0000 | 0.0000（确实为 0） |

修正值已用 **pycocotools 官方 COCOeval 交叉验证**（误差 <0.003），评估链路可靠。

---

## 二、优化成果总表

### 2.1 v11s 修复（P0）—— 从"废模型"到可用

| 步骤 | 操作 | 结果 |
|------|------|------|
| 诊断 | 输出张量 box(0~640) 与 cls(0~1) 混用单 scale=2.529 | cls 概率被量化压成 0 → mAP=0 |
| 修复① | ONNX 输出层 box 通道 ÷640 归一化（Slice+Div+Concat） | 输出值域统一 0~1 |
| 修复② | 输出布局 `[1,8400,84]`→Transpose→`[1,84,8400]`（绕开 rknn 对 axis=2 Slice 的 bug） | 转换无 CPU 回退 |
| 修复③ | 校准集 20→100 张 COCO | 量化统计更准 |
| **结果** | `yolov11s_norm2.rknn`（11.9MB, INT8） | **mAP 0 → 0.4527** |

### 2.2 校准集升级（P1）—— 结论：无益，保留原模型

| 模型 | 原版（20 校准） | 新版（100 校准+归一化） | 结论 |
|------|:---:|:---:|------|
| v5s | **0.5856** | 0.5842 (FP16) | 持平 |
| v8s | **0.6120** | 0.4959 (归一化) | **下降** |
| v11s | 0 | 0.4527 (修复) | 大幅提升 |

> 教训：v8s 的 6 张量解耦输出（各分支独立量化 scale）优于 Concat 融合输出（共用 scale 有量化损失）。**已验证可用的模型不要重转**。

### 2.3 Python 推理优化（P2）

| 优化 | 措施 | 效果 |
|------|------|------|
| uint8 直传 | 输入 `astype(uint8)` 免 float32 转换 | 41.6→36.4ms/次（**-12%**），输出完全一致 |
| 后处理向量化 | 逐元素循环改 numpy 批量运算 | 100 张图评估提速明显 |
| lite2 运行时 | rknn-toolkit-lite2 2.3.2 | 36.8ms 与 toolkit2 持平，**不采用**（无速度收益） |

### 2.4 批处理/并行（P3）

| 方案 | 结果 |
|------|------|
| 固定 batch=4 模型转换 | ❌ 不可行：onnxruntime 图验证对 batch 修改的 Shape/Reshape 失败 |
| **Python 多实例并行**（4 线程） | ✅ **14.8 → 66.6 FPS（4.5× 加速）**，rknn.inference 释放 GIL |

---

## 三、最终推荐组合（性能 × 精度）

| 模型 | 三核延迟 | 单核延迟 | mAP@0.5 | 端到端 FPS | 结论 |
|------|:---:|:---:|:---:|:---:|------|
| **v5s**（原版 INT8） | 33.0ms | 29.1ms | **0.5856** | ~80（C++ 4线程）/ 66.6（Python 4线程） | 🏆 速度优先 |
| **v8s**（原版 INT8） | 39.2ms | 44.0ms | **0.6120** | ~87（C++ 6线程） | 🏆 精度最高 |
| **v11s**（修复版） | 84.0ms | 80.5ms | **0.4527** | ~11 单流 | 精度修复但速度硬伤 |

---

## 四、关键经验（踩坑记录）

1. **COCO category_id 有跳号**（80 类分布在 1~90），评估必须用真实 id 映射，否则 mAP 类别错位
2. **rknn 2.3.2 对输出层 axis=2（最后一维）的 Slice 有 bug**（通道被置 0），需先 Transpose 到 axis=1
3. **融合输出（box+cls 共 scale）INT8 量化损失大**：box 0~640 与 cls 0~1 混用是 v11s 原始 mAP=0 的根因；解耦多输出（v8s 6 张量）更优
4. **rknn 2.3.2 对 v5s 解码版 ONNX 的 INT8 量化会置零 obj/cls**（FP16 正常）——v5s INT8 需用 logit 版 ONNX
5. **mmse 量化 + 100 校准在板端不可行**（22+ 小时），normal 量化 100 校准仅需 3~8 分钟
6. **固定 batch 模型转换不可行**（onnxruntime 图验证限制）；批处理替代方案是**多实例并行**（C++ 线程池 79~87 FPS、Python 多线程 66.6 FPS）
7. **lite2 无速度优势**（36.8 vs 36.4ms）

---

## 五、产物清单（板端）

| 产物 | 路径 | 说明 |
|------|------|------|
| v11s 修复模型 | `/home/firefly/model/yolov11s/yolov11s_norm2.rknn` | mAP 0→0.4527 |
| v11s 修复 ONNX | `/home/firefly/model/yolov11s/yolov11s_T_norm2.onnx` | box 归一化 + Transpose |
| 评估脚本 | `/tmp/eval_detect.py` | 三模型批量推理（uint8 直传 + category_id 映射） |
| mAP 计算 | `/tmp/eval_map.py` | 101 点插值 mAP@0.5 |
| COCO 子集 | `/tmp/coco_eval/`（100 图 + 标注 + 校准集） | 评估与校准共用 |
| 100 张校准集 | `/tmp/coco_eval/dataset_100.txt` | 校准集升级 |

---

## 六、结论

1. **v8s 是最终最优选**：精度最高（0.6120）、端到端 87 FPS
2. **v11s 已修复**：从完全不可用（mAP=0）到 0.4527，代价是速度（84ms，C2PSA 结构 NPU 不友好）
3. **Python 链路优化成果**：uint8 直传 -12% + 多线程并行 4.5×（66.6 FPS）
4. **评估基建已可靠**：pycocotools 交叉验证过，后续所有精度数据可直接采信

## 当前统一 500 张验收基线（2026-09-02）

后续精度判断统一采用 COCO val2017 500 张子集（`/tmp/coco_eval_500/`，标注 `coco_val_subset_500.json`，列表 `image_list_500.txt`）。此前 100 张结果保留为历史阶段记录，不作为当前验收结论。

| 模型 | AP@[0.5:0.95] | FPS | 用途 |
|---|---:|---:|---|
| ONNX FP32 | 48.72% | — | 精度基准 |
| RKNN FP16 | 48.65% | 5.63 | 部署精度基准 |
| 当前 INT8 | 15.6%～16.3% | 约 16.5 | 待修复 |

INT8 第一阶段验收目标：AP ≥45%，且与 RKNN FP16 的差距不超过 3 个百分点。执行优先级为：输入 dtype 验证 → 原始多尺度检测头 → C++ float32 DFL/解码 → FP16 对齐 → 默认 INT8/MMSE/per-channel → 逐层 hybrid quant → 必要时 QAT。
## 2026-09-02 原始解耦头首轮实测

板端从 RKNN Model Zoo 获取 YOLO11 解耦 ONNX，输出为 9 个张量：三个尺度各包含回归 `[1,64,H,W]`、分类 `[1,80,H,W]`、score `[1,1,H,W]`。使用 500 张 `calibration_letterbox_500.txt` 校准并统一 114 灰边/conf=0.01/NMS=0.45 评测：FP16 AP@[0.5:0.95]=0.4861，INT8=0.4803，AP50/AP75=0.6531/0.5218。INT8 相比 FP16 下降 0.59 个百分点，超过第一阶段目标；纯 NPU Python 100 次推理约 15.42 FPS。

该结果说明主要问题确实来自融合输出图的量化方式，而不是 YOLO11 主干本身。后续优先查询 9 个输出的逐张量 `type/scale/zp`，再进行 MMSE、per-channel 和回归/分类 hybrid 消融。
## 2026-09-02 输入 dtype 验证结果

同一 `yolov11s_zoo_int8_letterbox500.rknn` 与同一张 640×640 RGB 输入对比：`uint8` 输入可正常推理；`float32` 输入触发 Toolkit2 错误 `current input size(4915200) > need input size(1228800)`。这确认该 INT8 模型要求 1,228,800 字节的 8-bit 输入，C++ 不能再无条件强制 float32 或依赖错误的 UINT8/INT8 解释。后续必须读取 `RKNN_QUERY_INPUT_ATTR` 的 `type/scale/zp`，分别实现 UINT8、INT8 手动量化和 pass-through 测试。
## 2026-09-02 三模型 500 张复测

使用同一板端评测脚本、500 张 COCO val2017 子集、114 灰边、conf=0.01、NMS=0.45：

| 模型 | AP@[0.5:0.95] | AP50 | AP75 | FPS |
|---|---:|---:|---:|---:|
| 原始解耦头 ONNX FP32 | 48.74% | 65.67% | 53.01% | — |
| 原始解耦头 RKNN FP16 | 48.61% | 65.67% | 52.98% | 5.91 |
| 原始解耦头 RKNN INT8 | 48.03% | 65.31% | 52.18% | 15.65 |

INT8 相比 FP16 下降 0.59 个百分点，满足 AP≥45% 且差距≤3个百分点的第一阶段目标。

## 2026-09-02 YOLOv5s / YOLOv8s / YOLO11s 同标准横向复测

统一实测条件：RK3588 板端、640×640、114 灰边 letterbox、RGB、uint8 NHWC、conf=0.01、NMS=0.45、500 张 COCO val2017 子集、官方 `pycocotools.COCOeval`，指标为 bbox AP@[0.50:0.95]。FPS 为板端 Python RKNN 纯推理吞吐（100 次、5 次 warmup），不含图像预处理和后处理。

| 模型 | 官方 COCO AP@[.50:.95] | 板端 AP@[.50:.95] | 板端 AP50 | 板端 AP75 | 板端纯 NPU FPS |
|---|---:|---:|---:|---:|---:|
| YOLOv5s | 37.4% | 41.28% | 59.91% | 45.01% | 5.81 |
| YOLOv8s | 44.9% | 45.35% | 61.57% | 48.80% | 16.90 |
| YOLO11s | 47.0% | 48.03% | 65.31% | 52.18% | 15.91 |

官方数据为 Ultralytics 640px、COCO val2017 单模型单尺度结果；官方速度是 CPU ONNX/A100 或 TensorRT 等不同平台数据，不能直接与 RK3588 NPU FPS 数值相减比较。板端三模型均为当前可用 RKNN INT8 产物，其中 YOLO11s 使用原始解耦检测头版本。
